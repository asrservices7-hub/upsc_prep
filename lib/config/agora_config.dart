class AgoraConfig {
  // TODO: Replace with your actual Agora App ID from console.agora.io
  static const String appId = '<YOUR_AGORA_APP_ID>';
  
  // For production, you should fetch the token from your backend server.
  // For testing, you can generate a temp token from the Agora Console.
  // Leave empty if your project is set to App ID only (no certificate).
  static const String token = '';
}
